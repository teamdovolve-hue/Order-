{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Mobile-first QR menu for restaurant tables. Customers browse the menu, enter their name and phone number, verify with Firebase Phone OTP, and place orders that sync to the billing panel through Firestore.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "vercel-build": "echo 'Static site — no build step needed'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "node-fetch": "^2.7.0"
  }
}
